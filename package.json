{
  "name": "nft-minting",
  "version": "1.0.1",
  "description": "A Template for ERC721 NFT Deployment on Monad Testnet",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network monadtestnet",
    "mint": "npx hardhat run scripts/mint.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.23.0",
    "inquirer": "^9.3.6"
  }
}
